overwrite: true
schema: "http://localhost:4000/graphql" # Mercurius Gateway URL

generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
      useTypeImports: true